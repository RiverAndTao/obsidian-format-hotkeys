---
description: 完成后提交推送；禁止 Cursor 作为 git 贡献者
alwaysApply: true
---

# Git 提交与推送

## 每次任务结束后

1. 完成本轮代码改动后，**必须**提交并推送到远程（`origin` 当前分支）。
2. 除非用户明确说不要提交/不要推送，否则默认执行：构建（如需要）→ 安装到库（如需要）→ commit → push。

## 禁止 Cursor 成为贡献者（硬性）

- **禁止**在 commit message 中出现任何 `Co-authored-by: Cursor` / `cursoragent@cursor.com` 行。
- **不要**使用会被 Cursor 自动注入 co-author trailer 的普通 `git commit` 作为最终提交。
- 使用仓库脚本提交并推送：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/git-commit-push.ps1 -Message "你的提交说明"
```

- 该脚本通过 `git write-tree` + `git commit-tree` 创建提交，再 `git reset --hard` 到新提交并 `git push`，从而绕过 Cursor 注入的 co-author。
- 推送前用 `git log -1 --format=%B` 确认正文中**没有** `Co-authored-by`。
- 若误带了 co-author 且尚未被他人基于该提交开发：用 `commit-tree` 重写后再推送（必要时 `--force-with-lease`，且仅限本仓库、用户已知晓的历史改写场景）。

## 作者信息

- Author / Committer 保持仓库原作者（`RiverAndTao` / 其邮箱），不要改成 Cursor。
